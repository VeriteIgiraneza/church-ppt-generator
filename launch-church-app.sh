#!/bin/bash
# Church PowerPoint Generator launcher for Linux
# Double-click this file (or run from terminal) to start the app.

# Change to the directory this script is in
cd "$(dirname "$0")"

# Start both servers via the root npm script
npm run dev